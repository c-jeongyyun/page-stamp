# Task Interaction Log

> 작업 요청 및 수정 히스토리

---

## 2026-05-06 — Task 0: 공통 준비 (메시지 타입 인터페이스 정의)

### 초기 요청

`src/types.ts` 신규 생성 — UI ↔ Sandbox 메시지 타입 및 공유 설정 타입 정의

**초기 결과물**
- `UiMessage`, `SandboxMessage` 유니온 타입
- `PluginSettings` 인터페이스 (sectionId, componentId, textLayerName, positioningMode, x, y, numberFormat, startNumber)
- `SectionInfo`, `ComponentInfo` 인터페이스

---

### 수정 1 — 네이밍: Figma 접두사 추가

**요청**: `SectionInfo`, `ComponentInfo` 앞에 `Figma` 키워드 추가

**결과**: `FigmaSectionInfo`, `FigmaComponentInfo`

---

### 수정 2 — 네이밍: `PluginSettings` → `PageStampConfig` → `PageStampMold`

**요청**: `PluginSettings`는 페이지 스탬핑 설정임을 반영한 네이밍 제안

**제안 후보**: `StampSettings`, `PageStampSettings`, `StampConfig`

**선택**: `PageStampConfig` (사용자 선택) → 이후 사용자가 직접 `PageStampMold`로 재변경

---

### 수정 3 — 네이밍: `NumberFormat` → `PagingFormat`

**요청**: `NumberFormat` → `PagingFormat`

**결과**: 타입명 및 인터페이스 내 필드명(`numberFormat` → `pagingFormat`) 일괄 변경

---

### 수정 4 — 구조: `x`, `y`를 `position`으로 감싸기

**요청**: `x: number`, `y: number` 필드를 `position` 객체로 감싸기

**결과**:
- `Position` 인터페이스 신규 추가 (`{ x: number; y: number }`)
- `PageStampMold`의 `x`, `y` 필드 → `position: Position` 단일 필드로 교체

---

### 수정 5 — `UiMessage` / `SandboxMessage` 방향 명확화 검토

**요청**: 두 타입명만으로는 메시지 방향을 알기 어렵다는 의견

**제안 후보**: `UiToSandboxMessage` / `SandboxToUiMessage`, `FromUiMessage` / `FromSandboxMessage`, `UiRequest` / `SandboxEvent`

**결정**: 현행 유지 (`UiMessage`, `SandboxMessage`) — 주석과 파일 구조로 방향을 명시하기로 함

---

### 수정 6 — 파일 분리: `src/types.ts` → `src/types/` 디렉토리

**요청**: 도메인 타입과 통신 타입을 별도 파일로 분리

**결과**:
- `src/types/domain.ts` — `FigmaSectionInfo`, `FigmaComponentInfo`, `Position`, `PagingFormat`, `PageStampMold`
- `src/types/messages.ts` — `UiMessage`, `SandboxMessage`
- `src/types/index.ts` — 전체 re-export
- 기존 `src/types.ts` 삭제

---

### 수정 7 — docs 반영

**요청**: 변경된 타입 네이밍을 docs에 반영

**결과**:
- `docs/tasks.md` — Task 0 완료 체크, `PluginSettings` → `PageStampMold`, `settings` → `mold` 전체 교체
- `docs/architecture.md` — 클래스 다이어그램, 파일 위치 테이블, 시퀀스 다이어그램 업데이트

---

### 수정 8 — CLAUDE.md 업데이트

**요청**: 디렉토리 구조 업데이트 + `PageStampMold` 도메인 개념 설명 추가

**결과**:
- Folder Structure에 `src/types/` 3개 파일 추가
- `## Domain Concepts` 섹션 신규 추가 — `PageStampMold` 필드별 테이블

---

### 커밋 이슈 및 재작업

**문제**: `/commit` 스킬 실행 시 mixed types 3개 그룹으로 분리 예정이었으나, 첫 번째 커밋에 모든 파일이 포함됨

**복구**: `git reset HEAD~1`로 커밋 취소 후 3개 커밋으로 재분리

| 커밋 | 파일 | 메시지 |
|---|---|---|
| 1 | `tsconfig.json` | `fix(config): correction of @figma/plugin-typings reference in tsconfig` |
| 2 | `src/types/` | `feat(types): addition of domain and message type definitions` |
| 3 | `CLAUDE.md`, `docs/` | `docs: update of project docs to reflect new type structure` |

**후속 조치**: `/commit` 스킬에 staging area 초기화 및 검증 단계 추가

---

## 관련 파일

- 트러블슈팅 상세: [`docs/trouble-shooting/260506.md`](docs/trouble-shooting/260506.md)
